{
  "name": "canopy-ai-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace root for @canopy-ai/sdk (TypeScript) and canopy-ai (Python). NOT published — this exists only to orchestrate per-workspace scripts via `npm run … --workspaces`. Publishing happens from each child package directory (e.g. `sdk/typescript/`).",
  "workspaces": [
    "typescript"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "npm run build --workspaces && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  }
}
