{
  "name": "pay_entity_registry",
  "description": "When `to` is a registry slug, pay() rejects it — slug-based pay was removed when entities became service-keyed. Agents pay addresses directly or call canopy.fetch(url) for service interactions.",
  "config": {
    "apiKey": "ak_live_test",
    "agentId": "agt_test",
    "baseUrl": "https://api.canopy.test"
  },
  "call": {
    "method": "pay",
    "args": { "to": "agentic.market/anthropic", "amountUsd": 0.05 }
  },
  "httpExchange": [],
  "expectedThrow": {
    "name": "CanopyError",
    "messageIncludes": "pay() accepts on-chain addresses only"
  }
}
